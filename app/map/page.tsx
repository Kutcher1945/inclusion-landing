import { MapPageClient } from "@/components/sections/map-page-client";

export const metadata = { title: "Карта объектов — Инклюзия" };

export default function MapPage() {
  return <MapPageClient />;
}
